import React from "react";
import "./Header.css";

const Header = props => (
  <div>
    <nav>
      <div className="nav-wrapper">Junction Dance Company</div>
    </nav>
  </div>
);

export default Header;
